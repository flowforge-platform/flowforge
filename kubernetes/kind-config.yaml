kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

name: flowforge

nodes:
  - role: control-plane
    extraPortMappings:
      # HTTP
      - containerPort: 80
        hostPort: 80
        protocol: TCP

      # HTTPS
      - containerPort: 443
        hostPort: 443
        protocol: TCP