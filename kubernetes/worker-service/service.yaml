apiVersion: v1
kind: Service

metadata:
  name: worker-service
  labels:
    app: worker-service

spec:
  selector:
    app: worker-service

  ports:
    - name: http
      protocol: TCP
      port: 8083
      targetPort: 8083

  type: ClusterIP