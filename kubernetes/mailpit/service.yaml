apiVersion: v1
kind: Service
metadata:
  name: mailpit

spec:
  selector:
    app: mailpit

  type: ClusterIP

  ports:
    - name: smtp
      port: 1025
      targetPort: 1025

    - name: web
      port: 8025
      targetPort: 8025