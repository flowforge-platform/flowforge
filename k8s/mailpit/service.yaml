apiVersion: v1
kind: Service
metadata:
  name: flowforge-mailpit
spec:
  type: NodePort
  selector:
    app: flowforge-mailpit
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: web
      port: 8025
      targetPort: 8025
      nodePort: 30025