spring:
  data:
    redis:
      host: localhost
      port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces